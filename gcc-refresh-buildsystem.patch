diff -up gcc-16-20260530/Makefile.in.1~ gcc-16-20260530/Makefile.in
--- gcc-16-20260530/Makefile.in.1~	2026-05-31 00:32:17.000000000 +0200
+++ gcc-16-20260530/Makefile.in	2026-06-10 01:59:56.757353852 +0200
@@ -1052,7 +1052,8 @@ EXTRA_HOST_FLAGS = \
 	'WINDRES=$(WINDRES)' \
 	'WINDMC=$(WINDMC)' \
 	'CREATE_GCOV=$(CREATE_GCOV)' \
-	'PROFILE_MERGER=$(PROFILE_MERGER)'
+	'PROFILE_MERGER=$(PROFILE_MERGER)' \
+	$(if $(LIBTOOL),'LIBTOOL=$(LIBTOOL)')
 
 FLAGS_TO_PASS = $(BASE_FLAGS_TO_PASS) $(EXTRA_HOST_FLAGS)
 
@@ -1123,6 +1124,7 @@ EXTRA_TARGET_FLAGS = \
 	'XGCC_FLAGS_FOR_TARGET=$(XGCC_FLAGS_FOR_TARGET)' \
 	'STAGE1_LDFLAGS=$$(POSTSTAGE1_LDFLAGS)' \
 	'STAGE1_LIBS=$$(POSTSTAGE1_LIBS)' \
+	$(if $(LIBTOOL),'LIBTOOL=$(LIBTOOL)') \
 	"TFLAGS=$$TFLAGS"
 
 TARGET_FLAGS_TO_PASS = $(BASE_FLAGS_TO_PASS) $(EXTRA_TARGET_FLAGS)
diff -up gcc-16-20260530/Makefile.tpl.1~ gcc-16-20260530/Makefile.tpl
--- gcc-16-20260530/Makefile.tpl.1~	2026-05-31 00:32:04.000000000 +0200
+++ gcc-16-20260530/Makefile.tpl	2026-06-10 01:59:56.762482557 +0200
@@ -777,7 +777,8 @@ EXTRA_HOST_FLAGS = \
 	'WINDRES=$(WINDRES)' \
 	'WINDMC=$(WINDMC)' \
 	'CREATE_GCOV=$(CREATE_GCOV)' \
-	'PROFILE_MERGER=$(PROFILE_MERGER)'
+	'PROFILE_MERGER=$(PROFILE_MERGER)' \
+	$(if $(LIBTOOL),'LIBTOOL=$(LIBTOOL)')
 
 FLAGS_TO_PASS = $(BASE_FLAGS_TO_PASS) $(EXTRA_HOST_FLAGS)
 
@@ -848,6 +849,7 @@ EXTRA_TARGET_FLAGS = \
 	'XGCC_FLAGS_FOR_TARGET=$(XGCC_FLAGS_FOR_TARGET)' \
 	'STAGE1_LDFLAGS=$$(POSTSTAGE1_LDFLAGS)' \
 	'STAGE1_LIBS=$$(POSTSTAGE1_LIBS)' \
+	$(if $(LIBTOOL),'LIBTOOL=$(LIBTOOL)') \
 	"TFLAGS=$$TFLAGS"
 
 TARGET_FLAGS_TO_PASS = $(BASE_FLAGS_TO_PASS) $(EXTRA_TARGET_FLAGS)
